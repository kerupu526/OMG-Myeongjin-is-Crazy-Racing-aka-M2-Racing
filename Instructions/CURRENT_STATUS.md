# 현재 상태와 32차 작업 결과

기준일: 2026-07-13

## 온라인 레이스

- NGO Milestone 1·2a·2b는 실제 2인 ParrelSync 테스트까지 통과했다.
- Milestone 3도 완료했다. `com.unity.services.multiplayer` 2.1.3을 사용해 익명 인증, private 2인 Lobby 세션, Relay 방 코드 생성·참가를 `NetworkBootstrapUI`에 연결했다.
- 방 코드는 상대 접속 전까지 호스트에게 표시되고, 잘못된 코드·연결 실패는 재시도할 수 있다. ParrelSync마다 UGS 인증 프로필을 분리한다.
- 패키지/코드 수준 검증은 통과했다. 실제 Cloud 방 코드 2인 라이브 재검증은 다음 온라인 플레이테스트 때 수행한다.

## 아이템

- 13종 전체 로스터가 `ItemCatalog`의 명시적 `NetItemId`로 정의되어 로컬과 네트워크가 같은 데이터를 복원한다.
- PDF 설명·수치·행동 데이터가 카탈로그에 들어 있다. 안전한 12개 PNG는 `Resources/ItemSpriteLibrary.asset`을 통해 월드 픽업과 설치 공격 비주얼에 연결된다.
- `jindungongcheong.png`는 실존 국가 문장이므로 절대 참조·커밋하지 않는다. 황금방패는 `shield.png`에 금색 tint를 적용한 오리지널 표현을 사용한다.
- C4는 P 수동 기폭, 막대형 수류탄은 5m 감지/10m 폭발, 원자폭탄은 즉시 10km 판정, 러브레터는 하트 효과다. 기본 폭탄은 150초 보유 시 원자폭탄으로 바뀌며 충전 중 피격 시 즉시 폭발한다.
- 방패 정책: 기본은 폭탄·러브레터, 가시는 폭탄·러브레터·다이너마이트(다이너마이트 반사), 황금은 원자폭탄을 제외한 모든 공격을 1회 막는다.

## 레이스 규칙

- `VictoryCondition.SimpleFinish`: 먼저 목표 바퀴를 완주한 레이서가 승리한다.
- `VictoryCondition.StarBet`: 완주자 전체의 별점을 비교하고, 동점이면 완주시간으로 판정한다. 아무도 완주하지 못하면 무승부, 별점·시간까지 같으면 무승부다.
- 산소 부족·화상은 2인 경기에서는 상대 승리, 상대가 없는 경우에는 사유가 있는 무승부다. 원자폭탄은 즉시 무승부로 끝낸다.

## 확정된 밸런스와 규칙

- 인벤토리: 주 슬롯 → 보조 슬롯 순서로 채우며 둘 다 차면 새 아이템으로 주 슬롯을 교체한다.
- 막대형 수류탄 근접 감지 거리: 5m.
- 아프리카TV: 패시브 0/초, 피격 +20, 방송사고 +15, 멘탈 최대 시 2초 조작 불가.
- 네더요새: 기본 체온 +1/초, 일반 피격 +15, 용암 피격 +40, 용암 체류 +10/초, 80% 경고.

## 31차 작업 커밋

- `789bd39` Remove completed M2Net diagnostics
- `23a7d81` Add Relay room-code sessions for online races
- `3f55a73` Add complete item catalog and sprite library
- `b0c969f` Implement final item behaviors and shield policies
- `2329c20` Add star-bet and per-racer victory rules
- `8321b80` Finalize item and stage balance rules

## 32차 작업 — UI 실제 구현

- 로컬 스테이지의 개발용 텍스트 HUD를 정식 인게임 레이아웃으로 교체했다. 상단에는 바퀴·대전 상태·남은 시간을, 좌하단에는 주/보조 아이템 슬롯과 조작 힌트를, 하단에는 공통 스테이지 게이지 바를 배치한다.
- 아이템 슬롯은 `ItemSpriteLibrary`의 안전한 스프라이트와 tint를 그대로 사용하고, 선택 아이템의 `ItemDefinition.description`을 상세 패널에 표시한다. 빈 슬롯일 때도 다음 픽업에서 무엇이 표시되는지 안내한다.
- 비키니시티·아프리카TV·네더요새의 이전 개별 게이지 라벨과 차량 충돌 텔레메트리는 정식 HUD가 있을 때 숨긴다. 브리핑·카운트다운·결과 패널은 레이스 HUD와 분리되어 표시된다.
- `RaceHudPresentationTests`가 컴팩트 카드 크기, 공통 게이지 설치, 아이템 획득 뒤 실제 스프라이트·상세 설명 표시를 검증한다. 수동 Unity Play 모드에서도 브리핑 버튼 문구와 레이스 HUD의 비중첩 배치를 확인했다.

## 32차 작업 — 스피드전과 방 설정

- 방 생성 전 `RoomSettingsUI`에서 아이템전/스피드전을 전환하고, 아이템전에서는 1·3·5바퀴와 단순 완주/별점 내기를 순환 선택한다.
- 스피드전은 규칙상 5바퀴·단순 완주·최고 100km/h로 고정된다. 랜덤 트랙 아이템은 비활성화하고, 모든 참가자에게 기본 휘발유를 레이스 시작 시와 이후 5초 간격으로 슬롯을 거치지 않고 자동 분사한다.
- 호스트가 고른 모드·목표 바퀴·속도 제한은 `NetworkRaceManager`의 NetworkVariable로 클라이언트에도 동기화된다. 방 코드와 방 설정은 기존 상태 UI와 같은 고정 픽셀 가독성으로 키웠고, 로컬 HUD·공통 게이지도 같은 기준으로 확대했다.

## 32차 작업 — 정식 스테이지 아트 프리팹

- `Assets/Prefabs/Stage`에 번들 Kenney 모델 기반의 지형 바위·방송 타워·용암 바위·냉각 아치 프리팹을 생성하고, `StageArtPrefabLibrary`를 통해 런타임 스테이지 조립 코드가 참조한다.
- 비키니 시티의 지형 해저드와 네더 요새의 용암 테두리는 이제 충돌 프록시와 분리된 모델 프리팹을 렌더링한다. 아프리카TV 방송사고 구역에는 방송 타워, 네더의 냉각 구역에는 요새 아치를 추가했다.
- `BuildCheck.BuildStageArtPrefabs` 및 프리팹 스모크 테스트를 통과했고, PlayMode는 52건 중 51 통과·실패 0·기존 스크린샷 1건 inconclusive였다. 비키니 시티 Play 모드에서도 레이스 진입과 정식 HUD가 함께 정상 동작함을 확인했다.

## 32차 작업 — 아이템 파생형 가중치

- 계열 선택은 기존처럼 균등하고, 10%의 파생형 승급 확률도 유지한다. 파생형 안에서는 배열 순서에 의존하지 않는 명시적 합계 100 가중치로 선택한다.
- 가속: 슈퍼 휘발유 60, 해피버스데이투유 28, 재석 유 12. 공격: C4 23, 다이너마이트 28, 막대형 수류탄 22, 원자폭탄 4, 러브레터 23. 방어: 가시 방패 72, 황금방패 28.
- 원자폭탄은 공격 파생형의 4%이므로 전체 아이템 스폰에서는 약 0.4%가 된다. `ItemSpawnWeightTests`가 가중치와 각 경계 선택을 고정 검증한다.

## 다음 작업 — UI 실제 구현 후속 1차

- `NetworkBootstrap`의 런타임 메뉴는 1280×720 기준 `Scale With Screen Size`로 통일했다. 작은 Free Aspect 미리보기에서도 방 설정 패널과 방 만들기/참가 조작부가 함께 축소된다.
- `GameManager.LastRaceResults`가 종료 시 모든 참가자의 완주·시간·별점 결과를 보존한다. 결과 모달은 모드·승리 조건·최종 순위·완주시간·별점을 실제 데이터로 표시한다.
- 브리핑과 결과를 금색 테두리의 중앙 카드로 정리했고, 시작 버튼은 브리핑 카드 내부에 배치한다. PlayMode 54건은 53 통과·실패 0·기존 스크린샷 1건 inconclusive였다.

## 32차 작업 — UI 실제 구현 후속 2차

- `NetworkMenuUI`가 HTML의 공통 색·폰트·카드 톤을 적용한 메인, 방 만들기, 방 참가, 대기 로비, 아바타, 환경설정 화면을 런타임에 구성한다. 기존 Relay 방 만들기·참가 컨트롤은 이 화면 안으로 재배치되어 기존 서비스 호출을 유지한다.
- 아바타는 현재 레이서 이름과 3색 선택을 `PlayerPrefs`에 저장하고 메인·로비·결과의 로컬 레이서 표기에 반영한다. 상대 프로필 동기화와 세부 얼굴/장식 선택은 다음 작업이다.
- 환경설정은 마스터 볼륨과 전체 화면 값을 저장한다. 볼륨은 즉시 `AudioListener`에 적용하고, 전체 화면은 독립 실행 빌드에서 적용한다. BGM/SFX·그래픽·언어는 아직 구현하지 않았다.
- 결과 카드의 장문 2인·5바퀴 요약은 760×620 기준 카드 안에 들어가도록 검증했고, 레이스 HUD는 시야 확보를 위해 기존 정식 크기에서 10% 축소했다.
- 이번 후속 UI 직접 PlayMode 검증은 프로필 2건, 설정 1건, 메뉴 2건, 결과 3건이 통과했다. 이어서 전체 PlayMode 66건을 다시 실행해 66건 모두 통과했다.
- Unity 6에서 `InputTestFixture`가 활성 UI 입력 상태를 바꾸며 생기던 `statePtr` 예외는, 테스트별 가상 키보드만 생성·정리하도록 격리 방식을 바꿔 해결했다. 차량 런타임 입력 액션도 차량 파괴 시 해제한다.

## 32차 작업 — 온라인 레이스 HUD 정식화

- `NetworkRaceHUD`의 생성용 정보 라벨과 중앙 배너는 런타임에서 숨기고, 로컬 `RaceHUD`와 같은 1280×720·90% 스케일 기준의 카드형 레이아웃을 구성한다. 상단에는 내 바퀴·남은 시간·대전 상태를, 좌하단에는 주/보조 슬롯과 실제 아이템 스프라이트를, 하단에는 선택 아이템 상세와 조작·스피드전 자동 휘발유 안내를 배치한다.
- 브리핑·카운트다운과 레이스 종료는 별도의 중앙 카드로 표시한다. 종료 카드는 현재 동기화된 승패·무승부 사유·양쪽 바퀴를 표시하며, 상대 이름·아바타·순위·완주 시간·별점은 다음 온라인 결과 동기화 작업에서 연결한다.
- `NetworkRaceHudPresentationTests`가 기존 임시 라벨 비활성화, 공통 Canvas 스케일, 카드 크기, 아이템 아이콘, 카운트다운·결과 카드 생성을 검증한다. 추가 후 Unity 전체 PlayMode 67건이 모두 통과했다.

## 32차 작업 — 온라인 결과·프로필 동기화

- 각 클라이언트는 저장된 레이서 이름과 아바타 색을 서버에 제출하고, 서버는 실제 송신자 기준으로 호스트/참가자 프로필을 기록해 양쪽에 복제한다. 온라인 HUD의 `VS` 표기와 아바타 칩은 이제 상대 프로필을 사용한다.
- 서버는 종료 시 `GameManager.LastRaceResults`를 두 결과 행으로 복제한다. 완주 여부·완주 시간·별점·순위와 승리 조건이 동기화되며, 온라인 결과 카드는 두 레이서의 같은 최종 순위를 표시한다.
- `RaceResultOrdering`으로 로컬·온라인이 동일한 순위 규칙을 사용한다. 완주 우선, 별점 내기에서는 별점 우선/시간 차순, 완전 동점에서는 공동 1위로 처리한다.
- `RaceResultResolverTests`에 순위·동점·별점 우선 테스트를 추가했고, Unity 전체 PlayMode 70건이 모두 통과했다. 실제 Relay 2인 세션에서의 최종 확인은 인터넷 사용 허용 뒤 수행한다.

## 다음 우선순위

세부 실행 메모는 [`NEXT_WORK.md`](NEXT_WORK.md)에 기록한다.

1. Relay 방 코드로 실제 호스트·클라이언트 2인 라이브 테스트를 다시 수행한다.
2. UI 실제 구현의 후속 단계로, 온라인 결과 버튼 흐름·로비 스테이지/준비 동기화와 최종 해상도별 실제 화면을 구현·확인한다.
3. 비키니 시티 IP 리스킨 여부는 별도 결정이 필요하다. 이번 작업에서는 변경하지 않는다.

## 33차 작업 — UI 실제 구현 완성·스테이지 소품 재조정

- `UI 실제 구현 후속 1차`부터의 기록을 다시 점검했다. 이후 메뉴·프로필·환경설정, 온라인 HUD, 온라인 결과·프로필 동기화까지는 기존 커밋과 PlayMode 70건 통과로 확인됐다.
- 아바타는 6색·눈·입·볼·귀·모자·번호판을 저장하고, 실제 프로필/로비 카드의 얼굴 표현과 온라인 프로필 데이터에 동기화한다. 환경설정은 마스터/BGM/SFX·그래픽 품질·전체 화면·언어 선택을 저장하며, 선택 상태가 버튼에 즉시 표시된다.
- 온라인 결과 카드에는 `다시 하기`·`로비로`·`메인으로` 흐름을 넣었다. 다시 하기/로비로는 두 레이서가 같은 선택을 할 때만 서버가 라운드를 초기화하고, 로비는 방장 규칙·스테이지 선택과 양쪽 준비 확인 뒤에만 시작한다.
- 스테이지는 산소 버블·비키니 지형 해저드·네더 용암/가스트를 교차 차선과 회피 가능한 폭으로 조정하고, 아프리카TV의 타워·카메라·경고 표지는 트랙 바깥으로 재배치했다. 온라인 로비의 스테이지 선택은 충돌에 관여하지 않는 동기화된 테마 소품으로 즉시 보인다.
- 화살표/WASD 축 바인딩을 각각의 `1DAxis`로 분리했고, 봇·리플레이·자동 검증에도 쓰는 입력 오버라이드 경로를 추가했다. 전체 PlayMode는 76건 통과했다. `NetworkRace.unity`와 `Stage_BikiniCity.unity`의 사용자 미커밋 변경은 계속 제외한다.
- 독립 실행 창의 `ProjectSettings/ProjectSettings.asset`은 이미 `resizableWindow: 0`이며, 창 크기 조절 비활성화 상태를 유지한다.
- **진행 상태: ✅ 완료 — UI 실제 구현과 스테이지 소품 재조정, 컴파일 및 PlayMode 76건 검증까지 마쳤다.**

## 현재 세션의 인터넷 사용 제한

- 사용자가 인터넷 사용을 **테스트 수행에 한해서만** 명시적으로 허용했다. 구현·조사·외부 자료 수집에는 인터넷이나 외부 자료를 절대 사용하지 않는다.

---

## 34차 작업 — Computer Use UI 점검·로비 이탈 안정화

- Unity Hub에서 `NetworkRace` 씬을 실행해 메인 메뉴, 아바타, 환경설정, 방 생성, 호스트 로비까지 실제 화면으로 점검했다. 16:9 기준 UI가 정상 표시되고 실행 중 콘솔 오류·경고는 없었다.
- 다만 호스트를 종료하지 않은 채 Unity Play Mode를 끝내면 Multiplayer Services가 직접 `NetworkManager.Shutdown()`을 감지해 종료 경고를 남긴다. 또한 정식 로비에 사용자가 스스로 방을 나갈 UI 동선이 없다.
- 로비에 `방 나가기` 버튼을 추가하고, 호스트는 세션 삭제·클라이언트는 세션 이탈 API를 통해 NGO를 정상 종료하도록 바꿨다. 실제 호스트에서 방 나가기 후 메인 메뉴 복귀와 Play Mode 종료까지 다시 확인했으며 콘솔 경고·오류는 모두 0건이었다.
- `check_build.ps1` 컴파일 성공, `check_playtest.ps1` PlayMode 테스트 76/76 통과로 변경을 검증했다. 2인 동기화가 필요한 화면은 이번 UI 점검 범위가 아니어서 Clone 실행은 하지 않았다.
- **진행 상태: ✅ 완료 — Computer Use UI 점검과 로비 이탈 안정화, 컴파일 및 PlayMode 76건 검증까지 마쳤다.**

---

## 35차 작업 — Figma 재설계 기준 확정·UI Toolkit 실제 이행

- 실제 Unity 화면에서 카드·텍스트·아바타 요소가 겹치는 결함을 확인했다. 기존 런타임 uGUI 생성 코드는 화면 기준 디자인을 유지하기 어려우므로, 새 UI 구현은 UI Toolkit의 UXML/USS 구조로 전환한다.
- 사용자가 제공한 Figma 파일에는 업로드 아이콘을 사용한 메인·로비·HUD·결과·아바타·환경설정 시안이 들어 있다. 해당 시안을 새 시각 기준으로 삼고, 프로젝트의 `M2 레이싱 게임 UI 디자인` 폴더에 있는 원본 SVG/PNG를 Unity 자산으로 정식 반입한다.
- Figma 읽기 컨텍스트와 프로젝트 안의 원본 HTML 여섯 프레임을 대조해, 1280×720 기준의 실제 색·간격·폰트·아이콘 배치를 확정했다. 메인·방 생성·참가·로비·아바타·환경설정은 `M2Menu.uxml`/`M2Menu.uss`의 UI Toolkit 구조로 다시 작성했고, 화면별 SVG 그라데이션과 업로드 아이콘을 직접 바인딩했다.
- 사용자가 준 `M2.png`는 UI 로고가 아니라 Standalone 실행 파일 아이콘으로만 등록했다. `M2UiToolkitMenu`의 모든 UI 바인딩에서는 해당 이미지를 제거했고, `ProjectSettings`의 Standalone 8개 아이콘 슬롯이 모두 이 아이콘을 참조한다.
- Figma ③ 인게임 HUD와 ④ 결과도 `M2RaceHudView.uxml`/`M2RaceHud.uss`와 `M2RaceHudToolkit`으로 이행했다. 기존 `NetworkRaceHUD`는 레이스 데이터·입력 흐름을 보존한 비표시 폴백으로만 두고, 새 UI Toolkit 계층이 실제 아이템·레이서·순위·시간·별점·결과 버튼을 같은 복제 데이터에서 표시한다. HUD 상단 카드·부스터·아이템 슬롯·게이지·완주 안내와 결과 카드·장식 사진·아바타 표정·버튼 비율도 원본 프레임 좌표로 다시 보정했다.
- Figma 원본을 다시 읽어 메인·로비·아바타 화면의 누락 장식도 보완했다. 메인의 사진 3개, 아바타 눈 하이라이트와 프리셋 미니 아바타, 로비의 바나나·신호등 아이콘과 준비 상태 클래스가 UI Toolkit 구조에 반영됐다. `M2.png`는 이 화면들에 사용하지 않는다.
- 기본 해상도는 1280×720으로 고정했고 `resizableWindow: 0`을 유지했다. 네트워크 스테이지 테마 소품도 드라이버블 레인 바깥 좌표로 옮겨 장식이 주행선 안에 놓이지 않게 했다. 사용자 미커밋 씬은 재생성·저장하지 않았다.
- 직전 Unity 6 에디터 임포트와 C# 재컴파일은 성공했고, UXML XML 검증과 변경 경로 `git diff --check`도 통과했다. 다만 사용자 직접 입력 뒤 반영한 마지막 Figma 미세 보정과 새 HUD SVG는 Unity 에디터의 다음 정상 임포트에서 확인해야 한다. 안전상 사용자 재개 지시 전에는 에디터에 추가 입력을 보내지 않는다.
- Unity Hub로 실제 프로젝트를 열어 Computer Use 화면 캡처까지는 정상 복구됐으나, Play Mode 조작 직전에 사용자 직접 입력이 감지되어 Computer Use 입력을 즉시 중단했다. Unity 창에 추가 입력을 보내지 않았고, 2인 Clone 테스트도 실행하지 않았다.
- **진행 상태: ✅ 완료 — Figma 기준 UI Toolkit 구현·게임 아이콘·1280×720 고정·스테이지 테마 소품 재배치를 반영했으며, 후속 결함 수정과 실제 Play Mode 검증은 36차 작업에서 완료했다.**

---

## 36차 작업 — UI Toolkit 결함 전면 수정

- 사용자 제보 12개 항목을 기준으로 메인·방 참가·로비·아바타·환경설정 UXML/USS를 다시 보정했다. 카드·버튼·아이콘 그림자, 직사각형 모서리, 버튼 패딩과 입력 높이, 텍스트 수직 정렬, 창 밖 넘침을 Figma 원본 배치에 맞췄다.
- 아바타 프리셋 5개와 눈·입·볼터치·귀·모자·번호판을 실제 버튼으로 바꿨다. 프리셋과 개별 선택이 미리보기에 즉시 반영되며 볼터치·귀의 `없음`도 실제로 해당 요소를 숨긴다.
- 환경설정의 마스터/BGM/SFX 슬라이더, 그래픽 품질, 전체화면/창모드를 조작 가능한 컨트롤로 복구했다. 로비 모드·바퀴·스테이지·승리 조건도 연결 중 상태와 관계없이 선택할 수 있도록 직접 버튼으로 연결했다.
- 배경 장식에는 화면별 위상과 속도가 다른 부유·회전 애니메이션을 연결했다. 방 참가 폼과 버튼은 클릭 가능한 높이와 패딩을 확보했고, 로비 연결 상태는 `게임 설정` 제목과 분리했다.
- UXML XML 파싱, 모든 버튼 바인딩 이름 대조, `git diff --check`, 갱신 C#의 Unity 참조 컴파일을 통과했다. Unity 6 에디터의 자동 임포트와 실제 Play Mode에서 메인, 아바타 프리셋/입/볼터치/귀, 설정 슬라이더/품질/화면 모드의 반응을 확인했다.
- 실제 Play Mode에서 방 참가 입력·버튼 검증 안내, 테스트 호스트 생성, `M2-9AMJ` 형식의 방 코드, 로비 게임 모드·스테이지 선택 반응을 확인했다. 이후 사용자 직접 입력이 감지되어 Computer Use 입력은 즉시 종료했으며, 나머지 로비 선택은 동일한 직접 버튼 바인딩과 컴파일 검증으로 확인했다.
- Unity Editor 로그에는 최근 Tundra 빌드 성공만 기록됐고 실행 예외는 0건이었다. 방 코드 생성 500회를 별도 검증해 모두 `M2-[A-Z0-9]{4}` 형식을 만족했다. 사용자 미커밋 씬의 기존 공백 차이는 보존하고, 그 외 작업 파일의 `git diff --check`는 통과했다.
- **진행 상태: ✅ 완료 — 제보된 UI 결함 12개 수정, 실제 Play Mode 조작 확인, 호스트 로비 및 방 코드 테스트, 컴파일·로그·정적 검증을 마쳤다.**

---

## 37차 작업 — Computer Use UI 최종 재검증·스크린샷 증빙

- Unity Hub에서 열려 있던 `NetworkBootstrap` 프로젝트의 Play Mode를 다시 실행 상태로 연결해 메인, 방 참가, 실제 Relay 호스트 로비, 아바타, 환경설정 화면을 순서대로 확인했다.
- 로비에서 아이템전, 1바퀴, 네더 요새, 별점 내기 선택이 실제 버튼 입력에 따라 변경됐다. 테스트 방 코드는 `M2-9AMJ`로 표시됐으며 캡처 후 방 나가기를 통해 정상 종료했다.
- 아바타 설정은 화면 밖 넘침 없이 전체 컨트롤이 보였고 핑크 프리셋 선택 시 외형과 `#077` 이름이 즉시 반영됐다. 환경설정에는 세 개의 볼륨 슬라이더와 그래픽 품질·화면 모드 버튼이 모두 표시됐다.
- 전체 Unity 창 캡처와 16:9 Game View 크롭을 `TestArtifacts/UI/2026-07-14`에 저장하고, 다섯 핵심 화면과 프리셋 결과를 한 번에 보는 `GameView/00-ui-contact-sheet.png`를 생성했다.
- 최신 Tundra 빌드 성공 이후 C# 컴파일 오류나 런타임 예외는 없었다. 이전 로그의 HUD `StyleColor` 오류는 현재 소스에서 `Color` 중간값으로 이미 수정된 과거 기록임을 확인했다. Unity Play Mode도 최종 종료했다.
- **진행 상태: ✅ 완료 — 실제 UI 재조작, 로비 선택 반응, 테스트 방 정리, Game View 스크린샷 저장과 최신 로그 검증을 모두 마쳤다.**

---

## 38차 작업 — 소형 캡슐 세로 정렬 보정

- 사용자 확대 캡처에서 아바타 닉네임 입력, 로비 준비 배지, 상단 오락실 표지의 글자가 위로 붙거나 잘리는 현상을 확인했다. 원인은 Jua 폰트의 글리프 메트릭과 UI Toolkit의 기본 Label/TextField 안쪽 여백이 고정 높이와 맞지 않는 것이었다.
- 오락실 표지, 방 코드, 방장 배지, 준비 상태, 닉네임 입력에 명시적인 가로 너비·높이·반높이 radius를 지정해 원형이 아니라 캡슐 실루엣으로 통일했다. 내부 Label과 Unity 6 TextField의 실제 입력 요소에는 세로 여백 0과 `middle-center` 정렬을 적용했다.
- 실제 Play Mode에서 아바타 닉네임 입력과 상단 오락실 표지가 캡슐 중앙에 렌더링되는 것을 확인했다. 준비 상태는 USS만으로 런타임 Label이 내용 너비로 다시 줄어드는 것을 확대 캡처에서 발견해, `ApplyLobbyPlayer`에서 168×40 크기·줄바꿈 금지·중앙 정렬·축소 금지를 인라인으로 함께 고정했다.
- 마지막 C# 보정은 Unity Tundra 빌드 7.01초 성공으로 컴파일됐다. 최종 Play Mode 재실행 직전에 사용자 직접 입력이 다시 감지되어 Computer Use 입력은 즉시 중단했다.
- **진행 상태: 🔄 진행 중 — 닉네임 입력·오락실 표지의 실제 중앙 정렬은 확인했고 준비 캡슐의 런타임 강제 크기 보정도 컴파일했다. 사용자 재개 지시 후 준비 캡슐 최종 화면만 다시 캡처하면 완료된다.**

---

## 39차 작업 — UI 최종 상호작용·가독성 보정

- 사용자 최종 피드백을 기준으로 버튼 호버·클릭 모션, 스피드전 바퀴 제한의 비활성 표시, 아바타 이름 배치, 푸터·꾸미기 캡슐, 슬라이더 손잡이, 실제 조작키 안내, 선택 상태 색상, 로비 안내문 넘침과 레이스 화면 테스트 진입을 함께 보정했다.
- 모든 버튼에 0.12초 호버 떠오름·그림자 확대와 클릭 눌림 전환을 붙였고, 스피드전에서는 1·3바퀴 칩이 비활성 투명도와 회색 글자로 잠긴다. 메인 아바타 닉네임은 아바타 아래 진한 29px 텍스트로 옮기고 아바타 내부 번호판 표기는 숨겼다.
- `인평랜드` 푸터와 `꾸미기` 버튼을 반높이 radius 캡슐로 통일하고, 설정 슬라이더 손잡이는 32px 트랙 안에 들어가는 16px 원형으로 바꿨다. 조작키 안내는 실제 바인딩(Shift 드리프트·Ctrl 가속 아이템·E 공격/방어)으로 고치고 Jua 폰트 캡슐로 표시하며, `RaceFlowUI` 브리핑 문구도 같은 내용으로 수정했다.
- 그래픽 품질·언어·화면 모드 등 선택형 칩은 선택 시 배경색과 `is-selected` 그림자가 함께 바뀐다. 로비 안내문은 캡슐 안에서 줄바꿈·클리핑되게 고정했다.
- 메인 화면 우측에 `▶ 로컬 레이스 테스트` 버튼을 추가해 세션 없이 `Stage_BikiniCity`로 진입해 실제 레이스 상태를 확인할 수 있고, 해당 씬을 `EditorBuildSettings`에 등록했다.
- Unity 에디터 재컴파일(Tundra 빌드 성공, 오류 0건)과 헤드리스 PlayMode 전체 테스트 84건 전원 통과로 검증했다. 실제 Play Mode 화면 캡처는 사용자가 데스크톱을 사용 중이어서 보류했고, 다음 사용자 확인 때 스크린샷만 남기면 된다.
- **진행 상태: ✅ 완료 — 39차 UI 보정 9개 항목 구현, 컴파일·PlayMode 84건 검증까지 마쳤다. 화면 캡처 증빙만 사용자 재개 후 남긴다.**

---

## 40차 작업 — 아바타 얼굴 변형·모자 실구현·닉네임 태그 유지

- 사용자 제보 3건을 수정했다. ① 입 모양이 클릭해도 바뀌지 않던 원인은 USS의 `!important`였다. Unity USS는 `!important`를 지원하지 않아 변형 규칙이 통째로 무시됐고, 눈(웃는눈·선글라스) 변형도 같은 이유로 깨져 있었다. ② 모자는 텍스트 글리프(▰/♛)로만 그려져 있었다. ③ 닉네임을 저장하면 `#001` 태그가 사라졌다.
- 얼굴 변형은 Figma HTML `avatarHTML()`의 210px 기준 비율을 퍼센트 지오메트리로 옮겨 파일 끝의 우선 규칙 블록으로 재작성했다. 메인·로비·미리보기·프리셋 네 컨텍스트가 같은 규칙으로 스케일되며, 입(방긋/우와/시크), 웃는눈 아치, 선글라스 밴드(시안+핑크 렌즈)가 미리보기에 즉시 반영된다.
- 모자는 아바타 전체를 덮는 `avatar-hat` 오버레이 아래 캡모자(핑크 크라운+챙 도형 2개)와 왕관(`crown.svg` VectorImage)으로 실구현했다. UXML의 아바타 9개 인스턴스를 모두 새 구조로 바꿨고 `ApplyAvatar`가 표시 여부를 전환한다.
- `M2PlayerProfile.WithPlateTag`/`TaggedDisplayName`을 추가해 닉네임 뒤 번호판 태그를 항상 유지한다. 꼬리의 낡은 `#...` 토큰은 현재 번호판으로 교체되므로 번호판을 바꾸면 태그도 갱신된다. 메인·아바타 미리보기·로비 폴백·로컬 결과·온라인 프로필 제출(`NetworkRaceManager`)·UI Toolkit HUD가 태그 포함 이름을 쓴다.
- Unity Hub로 프로젝트를 열어 실제 Play Mode에서 확인했다: 입 3종·눈 3종·볼터치·왕관/캡모자 전환이 미리보기에 즉시 반영되고, 번호판 `#077` 선택 시 메인 카드가 `앙 #077`로 갱신됐다. 증빙은 `TestArtifacts/UI/2026-07-14-39th/`에 저장했다.
- **진행 상태: ✅ 완료 — 배치 컴파일 성공, 실제 Play Mode 화면 확인, 헤드리스 PlayMode 84건 전원 통과까지 마쳤다.**
